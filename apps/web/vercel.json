{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install --include=dev --workspaces --include-workspace-root",
  "buildCommand": "npm run build --workspace @classconnect/shared && npm run build --workspace @classconnect/db && npm run build --workspace @classconnect/api && npm run build --workspace @classconnect/web",
  "functions": {
    "src/pages/api/v1/[...path].ts": {
      "maxDuration": 30
    }
  },
  "crons": [
    {
      "path": "/api/v1/jobs/billing-pass",
      "schedule": "0 5 * * *"
    }
  ]
}
