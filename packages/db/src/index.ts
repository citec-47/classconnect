/**
 * Database client surface.
 *
 * Re-exported from one place so application code never imports the generated
 * client directly and the generator output location can change without a
 * cascade of import edits.
 */
export * from '@prisma/client';
export { PrismaClient } from '@prisma/client';
